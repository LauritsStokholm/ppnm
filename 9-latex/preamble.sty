% Typesetting, design and layout
% Essentials
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}

% Typography magic!
\usepackage[final]{microtype} % Fixes badbox problems. Compatible with pdflatex.

% Languages
\usepackage[english]{babel}
% Daleif recommends:
% Orddeling og ordentligt mellemrum efter forkortelser
%\renewcommand{\danishhyphenmins}{22} % bedre orddeling
\frenchspacing  % Sørger for ordentligt mellemrum efter forkortelser (e.g.)

% Fonts
\usepackage{lmodern}
%\usepackage{libertine} % Linux Libertine as text font
%\usepackage[libertine]{newtxmath} % Nicely scaled math font

% Margins
\setlxvchars
\setulmargins{*}{*}{1.618}
\setlrmargins{*}{*}{1}
\checkandfixthelayout[nearest] % Necessary for memoir to compile the settings correctly

% Figures and floats
\usepackage{graphicx}
\usepackage{enumerate} % Tillader argumenter i enumerate env.
\usepackage{url}

% Sektioner er højest prioriteret
 \usepackage{chngcntr}
     \counterwithout{section}{chapter}
     \counterwithout{figure}{section}
     \counterwithout{figure}{chapter}
     \counterwithout{table}{chapter}
     \counterwithout{equation}{chapter}

% Mathematics
% Mathematical characters
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{mathtools} % Daleif, \nuparrow, \xrightarrow[under]{over}

% SI-units
%\usepackage{siunitx}
%\sisetup{output-decimal-marker = {,},
%         separate-uncertainty=true,
%         multi-part-units=single,
%         exponent-product = {\cdot},
%         per-mode = fraction}

% Debuggin
%\usepackage{lipsum}
%\usepackage[margin, draft]{fixme} \fxusetheme{color}
% \fxnote (grøn), \fxerror (gul), \fxwarning (orange), \fxfatal (rød)

% Things with draft
% \usepackage{ifdraft}
% \usepackage[firstpage]{draftwatermark}


% Code
\usepackage{listings}
%%\usepackage{caption}
%\usepackage{minted}
%\setminted{style=pastie, breaklines=true}
%\newenviroment{code}{\captionsetup{type=listing}}{}

% References, ToC and Chapters

% Smart quotation
\usepackage{csquotes}

% URL's
\usepackage{url}


% Testing sections
% Sections
\setsecheadstyle{\Large\bfseries\sffamily\raggedright}
\setsubsecheadstyle{\large\bfseries\sffamily\raggedright}

% Figures & Tables on page with TOC
%\usepackage{tocloft}


% TOC går til subsection (Benytter nu istedet 'chapter' til sections og 'section' til subsections)
%\setcounter{tocdepth}{0}



% Bibliography
\usepackage[backend=biber,
%style=numeric,  % Citation style as [NUMBER]
style=authoryear-comp,  % Citation style as (AUTHOR YEAR)
sorting=nyt,     % Sort NAME-YEAR-TITLE
sortcites=true,
dashed=false,
maxcitenames=2,
maxbibnames=5,
uniquelist=false,
doi=false,
url=false,
isbn=false,
eprint=false,
hyperref=true]{biblatex}
\addbibresource{bibliography.bib}

%% Space in bibliography
%\setlength\bibitemsep{1.5\itemsep}

%% Change bib-order
%\DeclareNameAlias{sortname}{last-first}
%
%% Et al. på dansk
%\DefineBibliographyStrings{danish}{%
%  andothers = {et\addabbrvspace al\adddot}
%}
%
%% Title of bibliography and entry in toc
%\defbibheading{memoirbib}[Litteraturliste]{%
%  \chapter*{#1} \addcontentsline{toc}{chapter}{#1}}


% Referencing packages (in this order!)
\usepackage{varioref} % Brug \vref, til referencer i nærområdet.

% Hyperlinks i TOC, etc.
\usepackage[colorlinks=true, allcolors=black]{hyperref}
\usepackage{hypcap} 

% Smarte referancer
\usepackage{cleveref}

%% Macros
%% Matematik
%% Partiel Differential
\newcommand\diff[3][\partial]%
{\frac{#1 #2}{#1 #3}}
%\diff{f}{x}\diff[d]{x}{t}

%%Facit-komando
%\newcommand{\facit}[1]{\underline{\underline{#1}}}
%% \facit{a=b}
%
%% Seperation in equation
%\newcommand\eqsep{\ensuremath{\quad , \quad}}
%
%% Vectors (we are doing physics!)
%\renewcommand{\vec}[1]{\ensuremath{\boldsymbol{#1}}\xspace}
%
%% Variable name
%\newcommand{\var}[2]{\ensuremath{#1_{\textup{#2}}}\xspace}
%
%% Code names
%\newcommand\numpy{NumPy\xspace}
%\

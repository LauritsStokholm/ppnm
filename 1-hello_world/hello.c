#include <stdio.h> // Standard input/output   (i.e printf)
#include <math.h>  // Standard math functions (i.e. sqrt)

int
main (int argc, char **argv)
{
  double x = sqrt(2.0);
  printf("Hello, World!\n");
  printf("Look! A number: sqrt(2) = %lg\n", x);
  return 0;
}

